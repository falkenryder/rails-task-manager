<ul>
  <% @tasks.each do |task| %>
    <li> <%= link_to "#{task.title}", task %> <%= link_to "Delete", delete_path(task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></li>
  <% end %>
</ul>

<%= link_to "Create a new task", tasks_new_path %>
